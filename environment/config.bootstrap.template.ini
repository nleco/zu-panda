; this file contains info specific to the machine.  it should be copied to 'config.bootstrap.ini' (this should never be checked in).
; default template should be whatever production will be

; can be 'production', 'dev', 'test', 'staging'
[env]
environment=production